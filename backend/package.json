{
  "name": "backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "vitest"
  },
  "dependencies": {
    "@auth/core": "^0.41.1",
    "@hono/auth-js": "^1.1.1",
    "dotenv": "^17.3.1",
    "hono": "^4.12.7",
    "nanoid": "^5.1.7"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
